import { NotificationResponse } from './notification-response';

describe('NotificationResponse', () => {
  it('should create an instance', () => {
    expect(new NotificationResponse()).toBeTruthy();
  });
});
